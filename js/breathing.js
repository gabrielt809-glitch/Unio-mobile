/* Unio Base Organizada v1 */
/* ━━━━ BREATHING ━━━━ */
const BREATHS={box:{name:'Caixa',ico:'⬛',desc:'Equilíbrio e controle: inspire, segure, expire e segure no mesmo ritmo.',phases:[{label:'Inspire',sec:4,anim:'expand'},{label:'Segure',sec:4,anim:'hold'},{label:'Expire',sec:4,anim:'contract'},{label:'Segure',sec:4,anim:'hold'}]},calm478:{name:'4-7-8',ico:'🌙',desc:'Boa para desacelerar antes de dormir ou reduzir ansiedade.',phases:[{label:'Inspire',sec:4,anim:'expand'},{label:'Segure',sec:7,anim:'hold'},{label:'Expire lentamente',sec:8,anim:'contract'}]},coherence:{name:'Coerência',ico:'🌊',desc:'Ritmo calmo e contínuo: 5 segundos inspirando e 5 expirando.',phases:[{label:'Inspire',sec:5,anim:'expand'},{label:'Expire',sec:5,anim:'contract'}]},energy:{name:'Energia',ico:'⚡',desc:'Respiração curta para despertar. Use por poucos ciclos.',phases:[{label:'Inspire curto',sec:2,anim:'expand'},{label:'Expire curto',sec:2,anim:'contract'}]},relax:{name:'Relaxar',ico:'🍃',desc:'Expiração mais longa para sinalizar descanso ao corpo.',phases:[{label:'Inspire suave',sec:4,anim:'expand'},{label:'Expire longo',sec:6,anim:'contract'}]}};
function renderBreathMode(){if(!S.breathMode||!BREATHS[S.breathMode])S.breathMode='box';const cur=BREATHS[S.breathMode];const modes=document.getElementById('breathModes');if(modes){modes.innerHTML=Object.keys(BREATHS).map(k=>{const b=BREATHS[k];return`<button class="breath-mode-btn${S.breathMode===k?' on':''}" onclick="selectBreathMode('${k}')">${b.ico} ${b.name}</button>`;}).join('');}const desc=document.getElementById('breathDesc');if(desc)desc.textContent=cur.desc;const mini=document.getElementById('breathMini');if(mini)mini.innerHTML=cur.phases.map(p=>`<span>${p.label} ${p.sec}s</span>`).join('');const phase=document.getElementById('breathPhase');const txt=document.getElementById('breathTxt');const ring=document.getElementById('breathRing');if(phase)phase.textContent='';if(txt)txt.textContent='Toque para iniciar';if(ring)ring.classList.remove('expand','hold','contract','pulse');}
function selectBreathMode(id){if(!BREATHS[id])return;if(breathTimer)toggleBreath();S.breathMode=id;breathPhaseIdx=0;renderBreathMode();haptic('light');saveState();}
function toggleBreath(){const btn=document.getElementById('breathBtn'),ring=document.getElementById('breathRing'),ph=document.getElementById('breathPhase'),txt=document.getElementById('breathTxt');if(breathTimer){clearTimeout(breathTimer);breathTimer=null;ring.classList.remove('expand','hold','contract','pulse');btn.textContent='Iniciar';ph.textContent='';txt.textContent='Pausado';return;}btn.textContent='Parar';breathPhaseIdx=0;runBreathPhase();}
function runBreathPhase(){const cur=BREATHS[S.breathMode]||BREATHS.box;const ring=document.getElementById('breathRing'),ph=document.getElementById('breathPhase'),txt=document.getElementById('breathTxt');const p=cur.phases[breathPhaseIdx%cur.phases.length];ph.textContent=`${cur.ico} ${p.label}`;txt.textContent=p.sec+'s';ring.classList.remove('expand','hold','contract','pulse');ring.classList.add(p.anim);if(S.breathMode==='energy')ring.classList.add('pulse');breathTimer=setTimeout(()=>{breathPhaseIdx++;runBreathPhase();},p.sec*1000);}
