/* Unio Base Organizada v1 */
/* ━━━━ HOME ━━━━ */
function renderHome(){const now=new Date(),h=now.getHours();const nameStr=S_name?', '+S_name:'';const gr=(h<12?'Bom dia 🌅':h<18?'Boa tarde ☀️':'Boa noite 🌙')+nameStr;document.getElementById('homeGreeting').textContent=gr;document.getElementById('homeDate').textContent=`${DL[now.getDay()]}, ${now.getDate()} de ${MS[now.getMonth()]}`;const todayHabs=S.habits;const habRow=document.getElementById('homeHabitsRow');if(todayHabs.length){habRow.style.display='block';const done=todayHabs.filter(h=>h.log&&h.log.includes(dayKey(now))).length;document.getElementById('homeHabPct').textContent=`${done}/${todayHabs.length}`;document.getElementById('homeHabStrip').innerHTML=todayHabs.slice(0,8).map(h=>{const isDone=h.log&&h.log.includes(dayKey(now));return`<div class="hhab-dot" onclick="quickToggleHab(${h.id})"><div class="hhab-circle${isDone?' done':''}" style="background:${isDone?'rgba(52,199,89,.18)':'var(--bg3)'};">${h.emoji}</div><div class="hhab-lbl">${h.name}</div></div>`;}).join('');}else habRow.style.display='none';const wPct=Math.min(S.water.amt/S.water.goal*100,100).toFixed(0);const totalActs=S.health.acts.reduce((a,x)=>a+x.cal,0);const todaySleep=S.sleep[0]?S.sleep[0].h:null;const todayFoodCount=Object.values(S.nutr).flat().length;const todayTasks=S.tasks.filter(t=>t.date===dayKey(now));const doneTasks=todayTasks.filter(t=>t.done).length;const cards=[{id:'water',ico:'💧',title:'Água',val:S.water.amt+'ml',sub:`${wPct}% da meta`,bar:wPct,clr:'var(--teal)'},{id:'nutrition',ico:'🥗',title:'Nutrição',val:todayFoodCount+' itens',sub:'registros no diário',bar:Math.min(todayFoodCount/6*100,100),clr:'var(--green)'},{id:'health',ico:'🏃',title:'Saúde',val:S.health.steps.toLocaleString('pt-BR'),sub:'passos · '+totalActs+' kcal',bar:Math.min(S.health.steps/10000*100,100),clr:'var(--orange)'},{id:'sleep',ico:'🌙',title:'Sono',val:todaySleep?todaySleep+'h':'—',sub:todaySleep?'última noite':'sem registro',bar:todaySleep?Math.min(todaySleep/8*100,100):0,clr:'var(--purple)'},{id:'tasks',ico:'✅',title:'Tarefas',val:doneTasks+'/'+todayTasks.length,sub:'tarefas concluídas',bar:todayTasks.length?doneTasks/todayTasks.length*100:0,clr:'var(--blue)'},{id:'habits',ico:'🔥',title:'Hábitos',val:S.habits.filter(h=>h.log&&h.log.includes(dayKey(now))).length+'/'+S.habits.length,sub:'hábitos hoje',bar:S.habits.length?S.habits.filter(h=>h.log&&h.log.includes(dayKey(now))).length/S.habits.length*100:0,clr:'var(--orange)'}];document.getElementById('homeGrid').innerHTML=cards.map(c=>`<div class="hcard" onclick="switchTabById('${c.id}')"><div class="hcard-ico">${c.ico}</div><div class="hcard-title">${c.title}</div><div class="hcard-val" style="color:${c.clr}">${c.val}</div><div class="hcard-sub">${c.sub}</div><div class="hcard-bar"><div class="hcard-fill" style="width:${c.bar}%;background:${c.clr}"></div></div></div>`).join('');document.getElementById('homeFocusSub').textContent=`${S.focus.type} min · ${S.focus.sessions} sessões hoje`;const weekSumEl=document.getElementById('homeWeekSummary');if(weekSumEl){const wkSleep=S.sleep.slice(0,7);const wkActs=S.health.acts;const wkTasks=S.tasks.filter(t=>t.date&&t.date>=weekKeys()[0]);const wkDoneTasks=wkTasks.filter(t=>t.done);const wkHabRate=S.habits.length?Math.round(S.habits.reduce((a,h)=>{const done=weekKeys().filter(k=>h.log&&h.log.includes(k)).length;return a+done/7;},0)/S.habits.length*100):0;if(S.sleep.length||S.health.acts.length||S.tasks.length){weekSumEl.style.display='block';document.getElementById('wsBadge').textContent=now.getDay()===1?'📊 Resumo da semana':'📊 Esta semana';document.getElementById('wsContent').innerHTML=`<div style="display:flex;gap:8px;flex-wrap:wrap;">${wkSleep.length?`<div style="background:var(--bg3);border-radius:10px;padding:8px 12px;font-size:12px;font-weight:600;color:var(--purple);">🌙 ${(wkSleep.reduce((a,x)=>a+x.h,0)/wkSleep.length).toFixed(1)}h média sono</div>`:''}${wkActs.length?`<div style="background:var(--bg3);border-radius:10px;padding:8px 12px;font-size:12px;font-weight:600;color:var(--orange);">🏃 ${wkActs.length} atividades</div>`:''}${wkTasks.length?`<div style="background:var(--bg3);border-radius:10px;padding:8px 12px;font-size:12px;font-weight:600;color:var(--blue);">✅ ${wkDoneTasks.length}/${wkTasks.length} tarefas</div>`:''}${S.habits.length?`<div style="background:var(--bg3);border-radius:10px;padding:8px 12px;font-size:12px;font-weight:600;color:var(--orange);">🔥 ${wkHabRate}% hábitos</div>`:''}</div>`;}else weekSumEl.style.display='none';}}
