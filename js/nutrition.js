/* Unio Base Organizada v3 */
/* ━━━━ NUTRITION ━━━━ */
function norm(s){return String(s||'').toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'');}
const MEAL_MAP={b:{el:'mB',kc:'kcB',label:'Café da manhã'},l:{el:'mL',kc:'kcL',label:'Almoço'},d:{el:'mD',kc:'kcD',label:'Jantar'},s:{el:'mS',kc:'kcS',label:'Lanche'}};
function pickMeal(m,el){selMeal=m;document.querySelectorAll('.n-meal-btn').forEach(c=>c.classList.remove('on'));if(el)el.classList.add('on');}
function addDiaryFood(){const name=document.getElementById('foodNameInp').value.trim();if(!name)return;const note=document.getElementById('foodNoteInp').value.trim();const time=document.getElementById('foodTimeInp').value||new Date().toTimeString().slice(0,5);S.nutr[selMeal].push({id:Date.now(),n:name,note,time});document.getElementById('foodNameInp').value='';document.getElementById('foodNoteInp').value='';document.getElementById('foodTimeInp').value='';haptic('success');showToast('Registro adicionado ao diário','🥗');renderNutr();}
function rmFood(m,id){S.nutr[m]=S.nutr[m].filter(x=>x.id!==id);renderNutr();}
function renderNutr(){let total=0;Object.keys(MEAL_MAP).forEach(m=>{if(!Array.isArray(S.nutr[m]))S.nutr[m]=[];const items=S.nutr[m];total+=items.length;document.getElementById(MEAL_MAP[m].kc).textContent=items.length+' '+(items.length===1?'item':'itens');const el=document.getElementById(MEAL_MAP[m].el);el.innerHTML=items.length?items.map(it=>{const note=it.note?`<div class="food-note">${it.note}</div>`:(it.por?`<div class="food-note">${it.por}g${it.c?` · registro antigo com ${it.c} kcal`:''}</div>`:'');const time=it.time?`<div class="food-time">🕒 ${it.time}</div>`:'';return`<div class="food-item"><div style="flex:1;min-width:0;"><div class="food-nm">${it.n}</div>${note}${time}</div><button class="food-rm" onclick="rmFood('${m}',${it.id})">×</button></div>`;}).join(''):'<div class="empty" style="padding:12px;font-size:12px;">Vazio</div>';});const countEl=document.getElementById('nFoodCount');if(countEl)countEl.textContent=total;}
